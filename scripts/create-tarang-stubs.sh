#!/bin/bash
# Previously created stub crates for tarang path dependencies.
# No longer needed — tarang is now pulled from crates.io.
# Kept as a no-op for backwards compatibility with CI workflows.

echo "tarang is now a crates.io dependency, no stubs needed"
